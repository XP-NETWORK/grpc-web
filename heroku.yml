build:
  docker:
    web: Dockerfile

# run:
#   web: grpcwebproxy --run_tls_server=true --server_tls_cert_file="$LOCAL_CERT" --server_tls_key_file="$LOCAL_KEY" --backend_addr=167.172.180.70:9290 --backend_tls=false --allow_all_origins
  # web: ./entrypoint.sh
# release:
#   image: web
#   command:
#     -npm run migrate up